# CONTRIBUTING (organisation - défaut)

Merci de respecter le flux suivant :
- Créer une branche depuis `main` (`feature/...` ou `fix/...`).
- Ouvrir une PR avec le template fourni.
- S'assurer que lint + tests passent.
- Pas de secrets dans le code.
